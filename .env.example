# Example backend environment variables for SlideSage

# LLM / Provider keys
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here

# LiteLLM settings
# Model name to use with LiteLLM
LITELLM_MODEL=openai/gpt-3.5-turbo
# If you're running a LiteLLM proxy/gateway, set its base URL (optional)
LITELLM_PROXY_URL=http://localhost:4000

# Flask runtime flags
FLASK_DEBUG=True

# CORS origin
CORS_ORIGINS=http://localhost:8080

# Optional: Google Application Default Credentials (ADC)
# If your LLM provider uses Google Cloud (Vertex AI / Gemini), LiteLLM may
# rely on ADC to authenticate. Two recommended options:
# 1) Use `gcloud` to configure ADC (recommended for local development):
#    - Install Cloud SDK: https://cloud.google.com/sdk/docs/install
#    - Run: `gcloud auth application-default login`
# 2) Or set the path to a Service Account JSON key (less secure if committed):
#    - Create/download key.json from Google Cloud Console
#    - Set the env var in your shell or in `.env` (do NOT commit the JSON):
#      `export GOOGLE_APPLICATION_CREDENTIALS="/absolute/path/to/key.json"`
# To test ADC in your virtualenv run:
#    python -c "import google.auth; creds, project = google.auth.default(); print(project)"
